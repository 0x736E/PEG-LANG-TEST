function myfunc ( var1 ) {

    // this is a comment
    var xyz = (123 + ('456' + 'string') * var1)

    /*
    test
    */

    if ( true ) {
        // comment
    } else ( false ) {
        // another comment
    }

}